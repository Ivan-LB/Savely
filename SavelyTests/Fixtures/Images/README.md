# Synthetic receipt images

`synthetic-oxxo.png` is RENDERED by a script (Courier text on a cream
background), not a photo of a real receipt — it exists only so
`ReceiptOCRIntegrationTests` can run the real Vision → ReceiptParser
pipeline on CI without a camera. Never commit photos of real receipts here.
